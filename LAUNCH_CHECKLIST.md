# 上架 / 正式上线前待办清单

在把 CS Agent 正式部署到 Zendesk、给客服使用之前，建议按下面几类把工作收尾。

**当前进度**：本地开发与配置（key、OpenRouter、提示词可配置）已就绪；上线前主要差：后端正式部署、Zendesk 安装与 backend_url、真实 FAQ、鉴权与验收。

---

## 一、部署与配置（必做）

| 项 | 说明 | 状态 |
|----|------|------|
| 后端正式部署 | 将 `backend/` 部署到有 **HTTPS** 的服务器或云函数（如自己的服务器、Vercel、Railway 等），确保 Zendesk 能访问 | ☐ |
| 配置大模型密钥 | 生产环境用环境变量：`OPENAI_API_KEY`；OpenRouter 时还可设 `OPENAI_API_BASE`、`OPENAI_MODEL`。本地可用 `backend/config.json` 默认 key | ☐ |
| 在 Zendesk 安装 App | 用 `zcli apps:pack` 打 zip，在 Admin → Apps 上传安装 | ☐ |
| 配置 backend_url | 安装/编辑 App 时，把 **backend_url** 填成上面后端的公网地址（如 `https://your-backend.example.com`） | ☐ |

---

## 二、内容与知识库（必做 / 建议）

| 项 | 说明 | 状态 |
|----|------|------|
| 替换真实 FAQ | 把 `product_docs/faq.json` 改成你们真实产品与话术，或接入内部文档/Confluence | ☐ |
| 可选：历史工单知识库 | 若要做 RAG，需把历史工单导入向量库，并在后端 suggest-reply/summarize 里加检索逻辑 | ☐ |

---

## 三、安全与合规（建议）

| 项 | 说明 | 状态 |
|----|------|------|
| 后端鉴权 | 校验请求来源（如 Zendesk 请求带 token、或 IP/Referer 白名单），避免接口被滥用 | ☐ |
| 密钥不落库 | 生产用环境变量；本地默认 key 在 `backend/config.json`（已 gitignore），勿提交真实 key | ☐ |
| 隐私与合规 | 确认工单内容发给 LLM/第三方是否符合公司及客户政策，必要时做脱敏或本地化部署 | ☐ |

---

## 四、体验与兜底（可选）

| 项 | 说明 | 状态 |
|----|------|------|
| 错误与超时 | 接口超时、LLM 限流时的提示与重试（前端已有部分错误文案，可再统一） | ☐ |
| 按钮/界面随语言切换 | 若希望「客服偏好语言」切换后，按钮（如「使用并发送」）等也变成对应语言，需加一套多语言文案与切换逻辑 | ☐ |
| 加载与空状态 | 工单无内容、无评论时的展示是否清晰 | ☐ |

---

## 五、测试与验收（必做）

| 项 | 说明 | 状态 |
|----|------|------|
| 在真实 Zendesk 子域安装 | 在你实际使用的 Zendesk（如 rymindinc.zendesk.com）安装并打开工单，确认侧栏正常展示 | ☐ |
| 关联工单与权限 | 切换不同工单，确认侧栏内容随当前工单变化；确认客服/权限不足时行为符合预期 | ☐ |
| 全流程走通 | 优先级、翻译、工单总结与回复要点、建议回复、使用并发送/填入回复框，整条链路跑通 | ☐ |
| 与客服验收 | 让实际使用的客服试一轮，收反馈并修 bug | ☐ |

---

## 六、文档与运维（建议）

| 项 | 说明 | 状态 |
|----|------|------|
| 内部使用说明 | 给客服/管理员一页：如何打开侧栏、如何改偏好语言、如何用建议回复等 | ☐ |
| 监控与告警 | 后端接口可用性、LLM 调用失败率或限流，是否需要简单监控/告警 | ☐ |

---

## 小结：最小可上架范围

- **必做**：后端正式部署 + HTTPS、配置 LLM 密钥、Zendesk 安装 App 并配置 backend_url、替换真实 FAQ、在真实 Zendesk 上测试并验收。
- **强烈建议**：后端鉴权、密钥用环境变量、合规确认。
- **可选**：RAG 历史工单、按钮随偏好语言切换、监控告警。

按上面清单逐项打勾后，就可以视为具备「上架」/正式上线条件；若你指的是上架到 **Cursor** 或其它平台，可以说明具体平台，我再按该平台列一份对应清单。
