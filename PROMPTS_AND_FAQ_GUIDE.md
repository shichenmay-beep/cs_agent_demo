# 内容准备说明：FAQ 知识库 + 提示词

上线前「内容」这块建议准备两样：**知识库内容（FAQ）** 和 **提示词（如何用这些内容、语气与规范）**。

---

## 一、FAQ 知识库内容（必做）

**文件**：`product_docs/faq.json`

**作用**：生成「建议回复」和总结时，大模型会参考这里的条目，避免乱编或漏掉你们的标准话术。

**需要你准备的**：

1. **product_name**  
   你们的产品/品牌名，便于在提示里引用。

2. **faqs**  
   数组，每项两条：
   - **topic**：主题（如「退换货」「物流」「账号」）。
   - **content**：对应话术或政策原文（可直接给客服照抄或稍改的句子）。

3. **tone**  
   一句总体语气说明，例如：  
   `"Professional, friendly, and concise. Reply in English for US customers. Avoid jargon."`  
   会作为默认「回复风格」参与生成。

**示例**（按你们业务改）：

```json
{
  "product_name": "你们品牌名 (Shopify/Amazon)",
  "faqs": [
    {
      "topic": "退换货",
      "content": "支持签收后 30 天内退换；客户需在订单页发起退货；我们收到商品后 5–7 个工作日内退款。"
    },
    {
      "topic": "错发/瑕疵",
      "content": "若错发或商品有质量问题，请回复订单号与照片，我们可安排换货或全额退款。"
    }
  ],
  "tone": "专业、友好、简洁。美国客户用英文回复；避免行话，用简单句。"
}
```

有内部文档（Confluence / 飞书等）时，可以把整理好的话术导出或抄成上述格式，再贴进 `faq.json`。

---

## 二、提示词（建议准备）

**文件**：`product_docs/prompts.json`（可选）

**作用**：在不改代码的前提下，统一「优先级判断」「回复生成」「工单总结」「翻译」的规则和话术要求。  
后端会优先读取这里的字段；若某字段为空则用代码里的默认提示。

**需要你准备的**（按需填写，不必全填）：

| 字段 | 用在哪儿 | 建议写什么 |
|------|----------|------------|
| **priority.instruction** | 工单优先级评估 | 你们对 High/Medium/Low 的界定（例如：什么算紧急、什么算一般咨询），以及 reason 要强调的点（如「必须英文」）。 |
| **reply.instruction** | 建议回复生成 | 对回复的通用要求：是否必须引用 FAQ、禁止出现的内容、长度与段落数、是否不能暴露是 AI 等。 |
| **summary.instruction** | 工单总结与回复要点 | 对「工单总结」和「回复要点」的格式与侧重点（例如：总结要包含订单号/渠道，回复要点要包含必提政策）。 |
| **translate.instruction** | 最后一条会话翻译 | 对翻译的额外要求（例如：只输出译文、不要解释）。 |

**示例**（按你们规范改）：

```json
{
  "priority": {
    "instruction": "High：涉及退款/法律/强烈情绪；Medium：退换/物流/订单查询；Low：一般咨询。Reason 用一句英文说明原因。"
  },
  "reply": {
    "instruction": "优先引用 FAQ 中的政策；不要承诺具体到货日期；不要暴露是 AI；2–4 段、每段 2–3 句。"
  },
  "summary": {
    "instruction": "summary 写出客户诉求与订单/产品关键信息；replyPoints 写出必回应的点、必提政策、建议语气。"
  }
}
```

侧栏里客服填的「回复修改建议」是**单条工单级**的补充说明；`prompts.json` 是**全局默认**的提示词，两者会一起生效（全局 + 当条补充）。

---

## 三、准备顺序建议

1. **先做 FAQ**  
   把 `product_docs/faq.json` 换成你们真实产品与话术（可先做 5–10 条核心 FAQ + tone）。

2. **再补提示词**  
   若希望统一优先级标准、回复风格、总结格式，再在 `product_docs/prompts.json` 里填对应 instruction；不需要的可留空。

3. **上线前检查**  
   用几条真实工单跑一遍「建议回复」和「工单总结」，看是否符合话术与合规要求，再微调 FAQ 或 prompts。

这样「内容」这块就既有知识库，又有可维护的提示词，便于后续迭代。
